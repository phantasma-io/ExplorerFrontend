import React from 'react';

export interface DetailsDecimalsProps {
  amount: string;
  decimals: number;
}

export const DetailsDecimals: React.FC<DetailsDecimalsProps> = ({
  amount,
  decimals,
}) => {
  return null;
};
