import React from 'react';
import { Box } from '@mui/material';

export interface TableDisplayMobileProps {
  total?: number;
}

export const TableDisplayMobile = ({}: TableDisplayMobileProps) => {
  return <Box>MOBILE</Box>;
};
